# OSX
#
.DS_Store

# Xcode
#
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata
*.xccheckout
*.moved-aside
DerivedData
*.hmap
*.ipa
*.xcuserstate
ios/SousChefRN */
**/.xcode.env.local

# Android/IntelliJ
#
build/
.idea
.gradle
local.properties
*.iml
*.hprof
.cxx/
*.keystore
!debug.keystore
.kotlin/

# FCM push config — injected in CI from the GOOGLE_SERVICES_JSON_BASE64 secret
# (see .github/workflows/build-android.yml). Each dev keeps their own local copy.
android/app/google-services.json
ios/GoogleService-Info.plist

# node.js
#
node_modules/
yarn.lock
npm-debug.log
yarn-error.log

# fastlane
#
# It is recommended to not store the screenshots in the git repo. Instead, use fastlane to re-generate the
# screenshots whenever they are needed.
# For more information about the recommended setup visit:
# https://docs.fastlane.tools/best-practices/source-control/

**/fastlane/report.xml
**/fastlane/Preview.html
**/fastlane/screenshots
**/fastlane/test_output

# Bundle artifact
*.jsbundle

# Bundle analysis output
output.js
output.js.map

# Ruby / CocoaPods
**/Pods/
/vendor/bundle/

# Temporary files created by Metro to check the health of the file watcher
.metro-health-check*

# testing
/coverage
.eslintcache

# Yarn
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

*.env
.env.local
.env.production
.env.staging
.env.development
# react-native-config codegen
ios/tmp.xcconfig
.claude/
.codex/
.mcp.json
.vscode/mcp.json
.agents/
openspec/

artifacts/
e2e/artifacts/

# GraphQL codegen output (regenerated in CI; schema.graphql is the source of truth and stays tracked)
**/*.generated.ts
src/graphql/generated/schemaTypes.ts
src/graphql/generated/fragmentMatcher.json

# Generated build-time config (see scripts/generate-env.js)
src/config/env.generated.ts

# Audits are scratch by default, but the perf/offline baseline is tracked: it
# holds the measurement protocol, the numbers, and — more importantly — the
# retractions of earlier readings that were artifacts. Untracked, each session
# re-derives it and repeats the same mistakes.
docs/audits/*
!docs/audits/perf-offline-baseline-2026-08-24.md
!docs/audits/perf-ios-baseline-2026-08-25.md

# Reassure measurements. Per-run and per-machine — a baseline is only
# comparable against a current measured on the SAME machine minutes later,
# so there is nothing here worth committing.
.reassure/

# Watchman probe files. Watchman drops these into the watched root to verify it
# is receiving filesystem notifications, then deletes them; one is left behind
# whenever a watch dies mid-check. Transient, machine-named, never ours to
# commit.
.watchman-cookie-*
