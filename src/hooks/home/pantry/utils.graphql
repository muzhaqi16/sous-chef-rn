# Slice fragment for cache.updateFragment writes against Pantry.stats counters.
# Consumed by adjustPantryTotalItemsCount in utils.ts; not spread by any
# operation. Keep the selection minimal so updateFragment reads/writes only
# the counter being adjusted.
fragment pantryStatsCounter_pantry on Pantry {
  id
  stats {
    totalItems
  }
}
