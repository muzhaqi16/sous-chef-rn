/**
 * Folders the user cannot rename or delete — `Favorites` is server-maintained
 * for every account. Lives here rather than in the generic `FolderPicker`,
 * which must not carry one feature's vocabulary.
 */
export const PROTECTED_RECIPE_FOLDERS = ['Favorites'];
