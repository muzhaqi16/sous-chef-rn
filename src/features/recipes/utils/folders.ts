/**
 * Folders the user cannot rename or delete.
 *
 * `Favorites` is created and maintained by the server for every account. This
 * list used to be a `PROTECTED_FOLDERS` constant inside the generic
 * `FolderPicker`, which made a shared component carry one feature's vocabulary.
 */
export const PROTECTED_RECIPE_FOLDERS = ['Favorites'];
