# The id the search results read back off a barcode add. The mutation that
# selects it lives beside its consumer, `hooks/useAddScannedItem.graphql`.
fragment SearchResults_pantryItem on PantryItem {
  id
}
