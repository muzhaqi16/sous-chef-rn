# The ids the search results read back off a barcode add. The mutations that
# select them live beside their consumer, `hooks/useAddScannedItem.graphql`.
fragment SearchResults_shoppingListItem on ShoppingListItem {
  id
}

fragment SearchResults_pantryItem on PantryItem {
  id
}
