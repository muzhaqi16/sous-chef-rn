/**
 * Touch target constants
 * Consolidated from hardcoded values across components
 */

/**
 * Standard hit slop for touch targets
 * Increases tappable area by 8px on each side
 */
export const HIT_SLOP = { top: 8, bottom: 8, left: 8, right: 8 };

/**
 * Small hit slop for compact touch targets
 */
export const HIT_SLOP_SM = { top: 4, bottom: 4, left: 4, right: 4 };

/**
 * Large hit slop for important actions
 */
export const HIT_SLOP_LG = { top: 12, bottom: 12, left: 12, right: 12 };
