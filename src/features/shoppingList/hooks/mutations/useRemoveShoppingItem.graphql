# Narrow fragment for useRemoveShoppingItem's pre-eviction read.
# Only selects fields the hook actually reads from the cached entity
# before it's evicted — currently just `purchaseInfo.isPurchased` for the
# completedItems counter update.
fragment useRemoveShoppingItem_item on ShoppingListItem {
  id
  purchaseInfo {
    isPurchased
  }
}
