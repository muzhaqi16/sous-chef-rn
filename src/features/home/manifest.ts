import type { FeatureManifest } from '../types';

export const homeFeature: FeatureManifest = {
  id: 'home',
  // No tab — a home is reached through Profile > Home Management, and through
  // the join-by-code and join-by-link deep links.
};
