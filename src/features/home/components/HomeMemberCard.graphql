fragment HomeMemberCard_member on Membership {
  id
  homeId
  userId
  role
  status
  canManageHome
  canViewPantry
  canEditPantry
  canAddItems
  canRemoveItems
  canInviteOthers
  user {
    id
    email
    # Same gate as the shopping-list collaborator: an ACTIVE housemate reads
    # this whatever the other person's profile visibility says.
    displayName
  }
}
