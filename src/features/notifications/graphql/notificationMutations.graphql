# ============================================
# MUTATIONS
# ============================================

mutation MarkNotificationAsRead($input: MarkNotificationAsReadInput!) {
  markNotificationAsRead(input: $input) {
    __typename
    ... on MarkNotificationAsReadPayload {
      notification {
        id
        status
      }
    }
    ... on Error {
      code
      message
    }
    ... on NotFoundError {
      resource
      resourceId
    }
  }
}

mutation MarkNotificationUnread($input: MarkNotificationUnreadInput!) {
  markNotificationUnread(input: $input) {
    __typename
    ... on MarkNotificationUnreadPayload {
      notification {
        id
        status
      }
    }
    ... on Error {
      code
      message
    }
    ... on NotFoundError {
      resource
      resourceId
    }
  }
}

# Fire a self-addressed test notification — lets a user validate their
# notification setup (and, once remote push lands, that pushes arrive) from
# the settings screen.
mutation SendTestNotification($input: SendTestNotificationInput!) {
  sendTestNotification(input: $input) {
    __typename
    ... on SendTestNotificationPayload {
      notification {
        id
        title
        message
      }
    }
    ... on Error {
      code
      message
    }
  }
}

mutation DeleteNotification($input: DeleteNotificationInput!) {
  deleteNotification(input: $input) {
    __typename
    ... on DeleteNotificationPayload {
      notification {
        id
      }
    }
    ... on Error {
      code
      message
    }
    ... on NotFoundError {
      resource
      resourceId
    }
  }
}
