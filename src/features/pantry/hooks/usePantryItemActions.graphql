fragment usePantryItemActions_trackingUnit on PantryItem {
  id
  unit {
    id
  }
}

fragment usePantryItemActions_quantity on PantryItem {
  id
  quantity
  heldQuantity
}

fragment usePantryItemActions_id on PantryItem {
  id
}
