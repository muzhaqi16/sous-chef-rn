# ============================================
# UNIT CONVERSION QUERIES
# ============================================

query ConvertQuantity(
  $itemId: ID
  $quantity: Float!
  $fromUnitId: ID!
  $toUnitId: ID!
) {
  convertQuantity(
    itemId: $itemId
    quantity: $quantity
    fromUnitId: $fromUnitId
    toUnitId: $toUnitId
  ) {
    value
    displayText
    unit {
      id
      name
      symbol
      type
      displayAsFraction
      minPrecision
      autoConvertThreshold
    }
  }
}
