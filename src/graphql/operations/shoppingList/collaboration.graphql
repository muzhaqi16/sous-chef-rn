# ============================================
# QUERIES
# ============================================

query MyShoppingListInvites {
  me {
    id
    pendingCollaborationInvites {
      id
      token
      shoppingListId
      collaboratorId
      email
      role
      status
      canEdit
      canAddItems
      canRemoveItems
      canMarkPurchased
      canInviteOthers
      invitedAt
      expiresAt
      shoppingList {
        id
        name
        description
      }
      collaborator {
        id
        email
        profile {
          displayName
          avatar
        }
      }
      invitedBy {
        id
        email
        profile {
          displayName
        }
      }
    }
  }
}


# ============================================
# MUTATIONS
# ============================================

mutation AcceptShoppingListInvite($token: String!) {
  acceptShoppingListInvite(token: $token) {
    success
    message
    code
    collaborator {
      ...ShoppingListCollaboratorFragment
      canEdit
      canInviteOthers
      invitedAt
      shoppingList {
        id
        name
        description
      }
    }
  }
}

mutation DeclineShoppingListInvite($token: String!) {
  declineShoppingListInvite(token: $token) {
    success
    message
    code
    collaborator {
      id
    }
  }
}

# ============================================
# SUBSCRIPTIONS
# ============================================

subscription CollaborationChanges($shoppingListId: ID!) {
  collaborationChanged(shoppingListId: $shoppingListId) {
    changeType
    collaborator {
      ...ShoppingListCollaboratorFragment
      canEdit
      invitedAt
      lastViewedAt
    }
    shoppingListId
    timestamp
    userId
  }
}
