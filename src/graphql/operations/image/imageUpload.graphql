mutation CreateImageUploadUrl($input: CreateImageUploadUrlInput!) {
  createImageUploadUrl(input: $input) {
    __typename
    ... on CreateImageUploadUrlPayload {
      url
      key
      # The presigned POST policy. Every entry must be appended to the
      # multipart body before the file part, or object storage rejects the
      # upload — see uploadToObjectStorage() in src/hooks/useImageUpload.ts.
      fields {
        name
        value
      }
    }
    ... on Error {
      code
      message
    }
    ... on NotFoundError {
      resource
      resourceId
    }
    ... on ValidationError {
      field
    }
  }
}

mutation ConfirmProfileImageUpload($input: ConfirmProfileImageUploadInput!) {
  confirmProfileImageUpload(input: $input) {
    __typename
    ... on ConfirmProfileImageUploadPayload {
      url
    }
    ... on Error {
      code
      message
    }
    ... on NotFoundError {
      resource
      resourceId
    }
    ... on ValidationError {
      field
    }
  }
}

mutation ConfirmItemImageUpload($input: ConfirmItemImageUploadInput!) {
  confirmItemImageUpload(input: $input) {
    __typename
    ... on ConfirmItemImageUploadPayload {
      url
    }
    ... on Error {
      code
      message
    }
    ... on NotFoundError {
      resource
      resourceId
    }
    ... on ValidationError {
      field
    }
  }
}

# A profile avatar is set or removed through updateProfile (user.graphql):
# updateProfile(input: { avatar: $url }) or { avatar: null }.
