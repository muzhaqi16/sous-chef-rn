mutation SetDefaultHome($homeId: ID!) {
  setDefaultHome(homeId: $homeId) {
    success
    message
    code
    settings {
      id
    }
    defaultPantry {
      id
      name
      isDefault
    }
  }
}
