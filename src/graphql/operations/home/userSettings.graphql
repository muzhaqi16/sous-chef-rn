mutation SetDefaultHome($input: SetDefaultHomeInput!) {
  setDefaultHome(input: $input) {
    __typename
    ... on SetDefaultHomePayload {
      settings {
        id
      }
      defaultPantry {
        id
        name
        isDefault
      }
    }
    ... on Error {
      code
      message
    }
    ... on NotFoundError {
      resource
      resourceId
    }
    ... on ValidationError {
      field
    }
  }
}
