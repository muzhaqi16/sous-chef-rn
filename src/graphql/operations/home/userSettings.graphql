query GetDefaultHome {
  getDefaultHome {
    ...HomeFragment
  }
}

mutation SetDefaultHome($homeId: ID!) {
  setDefaultHome(homeId: $homeId) {
    id
    defaultHome {
      id
      name
    }
  }
}
