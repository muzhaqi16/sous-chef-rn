query SearchStores($search: String!, $limit: Int) {
  stores(filters: { search: $search }, first: $limit) {
    edges {
      node {
        id
        name
        address
      }
    }
    totalCount
  }
}
