# Skip tests when pushing only tags (e.g., npm run tag:playstore)
while read local_ref local_oid remote_ref remote_oid; do
  if echo "$local_ref" | grep -qv "^refs/tags/"; then
    npm run test:changed
    exit $?
  fi
done
