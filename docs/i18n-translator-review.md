# i18n — duplicate strings a translator needs to decide

Generated by the task 6.4 reconciliation. Each entry is one English string
that several keys declare, translated more than one way in the locale shown.
Capitalisation and punctuation drift was fixed mechanically; everything here
is a real choice about wording or grammar.

## Gender agreement — probably CORRECT as-is, do not "fix"

The same adjective agreeing with nouns of different gender. Unifying these
would replace a correct translation with a wrong one.

- **"Default"** (es)
  - `labels.default` → "Predeterminado"
  - `recipes.defaultBadge` → "Predeterminada"
  - `appearance.colorDefault` → "Predeterminado"
  - `appearance.fontDefault` → "Predeterminado"
  - `moveToPantry.defaultLabel` → "Predeterminada"
  - `homeManagement.cardDefault` → "Predeterminado"
  - `storageLocationCard.default` → "Predeterminada"
- **"None"** (es)
  - `recipes.none` → "Ninguna"
  - `shoppingListScreens.priorityNone` → "Ninguna"
  - `storageLocationForm.parentNone` → "Ninguna"
  - `storageLocationForm.tempNone` → "Ninguna"
  - `storageState.NONE` → "Ninguno"
  - `baseDimension.none` → "Ninguna"
  - `storageStateShort.NONE` → "Ninguno"
- **"Vegetarian"** (es)
  - `recipes.vegetarian` → "Vegetariana"
  - `recipes.diet.VEGETARIAN` → "Vegetariano"
  - `recipeFilters.diets.vegetarian` → "Vegetariana"
  - `addMealSheet.dietVegetarian` → "Vegetariano"
  - `dietaryProfile.diets.vegetarian` → "Vegetariana"
- **"Vegan"** (es)
  - `recipes.vegan` → "Vegana"
  - `recipes.diet.VEGAN` → "Vegano"
  - `recipeFilters.diets.vegan` → "Vegana"
  - `addMealSheet.dietVegan` → "Vegano"
  - `dietaryProfile.diets.vegan` → "Vegana"
- **"Lacto Vegetarian"** (es)
  - `recipes.diet.LACTO_VEGETARIAN` → "Lacto-vegetariano"
  - `dietaryProfile.diets.lactoVegetarian` → "Lacto-vegetariana"
- **"Ovo Vegetarian"** (es)
  - `recipes.diet.OVO_VEGETARIAN` → "Ovo-vegetariano"
  - `dietaryProfile.diets.ovoVegetarian` → "Ovo-vegetariana"
- **"Pescetarian"** (es)
  - `recipes.diet.PESCETARIAN` → "Pescetariano"
  - `recipeFilters.diets.pescetarian` → "Pescetariana"
  - `dietaryProfile.diets.pescetarian` → "Pescetariana"
- **"Expired"** (es)
  - `pantryItemDetail.batch.expired` → "Caducado"
  - `shoppingListScreens.statusExpired` → "Caducado"
  - `pantryAnalytics.reasonExpired` → "Caducado"
  - `filteredPantry.expired` → "Caducado"
  - `recordWaste.reasonExpired` → "Caducado"
  - `addToPantry.conditionExpired` → "Caducada"
  - `expiration.expired` → "Caducado"
- **"Custom"** (es)
  - `shoppingListScreens.patternCustom` → "Personalizado"
  - `storageLocationForm.typeCustom` → "Personalizado"
  - `saveAsTemplate.categoryCustom` → "Personalizada"
  - `templatePreview.customMeal` → "Personalizada"
- **"Spoiled"** (es)
  - `pantryAnalytics.reasonSpoiled` → "Estropeado"
  - `recordWaste.reasonSpoiled` → "Estropeado"
  - `addToPantry.conditionSpoiled` → "Estropeada"
- **"Invite"** (it)
  - `errors.entityInvite` → "Invito"
  - `homeManagement.cardInvite` → "Invita"
- **"Default"** (it)
  - `labels.default` → "Predefinito"
  - `recipes.defaultBadge` → "Predefinita"
  - `appearance.colorDefault` → "Predefinito"
  - `appearance.fontDefault` → "Predefinito"
  - `moveToPantry.defaultLabel` → "Predefinita"
  - `homeManagement.cardDefault` → "Predefinita"
  - `storageLocationCard.default` → "Predefinita"
- **"None"** (it)
  - `recipes.none` → "Nessuna"
  - `shoppingListScreens.priorityNone` → "Nessuna"
  - `storageLocationForm.parentNone` → "Nessuna"
  - `storageLocationForm.tempNone` → "Nessuna"
  - `storageState.NONE` → "Nessuno"
  - `baseDimension.none` → "Nessuna"
  - `storageStateShort.NONE` → "Nessuno"
- **"Vegetarian"** (it)
  - `recipes.vegetarian` → "Vegetariana"
  - `recipes.diet.VEGETARIAN` → "Vegetariano"
  - `recipeFilters.diets.vegetarian` → "Vegetariana"
  - `addMealSheet.dietVegetarian` → "Vegetariano"
  - `dietaryProfile.diets.vegetarian` → "Vegetariana"
- **"Vegan"** (it)
  - `recipes.vegan` → "Vegana"
  - `recipes.diet.VEGAN` → "Vegano"
  - `recipeFilters.diets.vegan` → "Vegana"
  - `addMealSheet.dietVegan` → "Vegano"
  - `dietaryProfile.diets.vegan` → "Vegana"
- **"Lacto Vegetarian"** (it)
  - `recipes.diet.LACTO_VEGETARIAN` → "Latto-vegetariano"
  - `dietaryProfile.diets.lactoVegetarian` → "Latto-vegetariana"
- **"Ovo Vegetarian"** (it)
  - `recipes.diet.OVO_VEGETARIAN` → "Ovo-vegetariano"
  - `dietaryProfile.diets.ovoVegetarian` → "Ovo-vegetariana"
- **"Pescetarian"** (it)
  - `recipes.diet.PESCETARIAN` → "Pescetariano"
  - `recipeFilters.diets.pescetarian` → "Pescetariana"
  - `dietaryProfile.diets.pescetarian` → "Pescetariana"
- **"Egg"** (it)
  - `recipes.intolerance.EGG` → "Uovo"
  - `recipeFilters.intolerances.egg` → "Uovo"
  - `dietaryProfile.intolerances.egg` → "Uova"
- **"Expired"** (it)
  - `pantryItemDetail.batch.expired` → "Scaduto"
  - `shoppingListScreens.statusExpired` → "Scaduto"
  - `pantryAnalytics.reasonExpired` → "Scaduto"
  - `filteredPantry.expired` → "Scaduto"
  - `recordWaste.reasonExpired` → "Scaduto"
  - `addToPantry.conditionExpired` → "Scaduta"
  - `expiration.expired` → "Scaduto"
- **"Custom"** (it)
  - `shoppingListScreens.patternCustom` → "Personalizzato"
  - `storageLocationForm.typeCustom` → "Personalizzato"
  - `saveAsTemplate.categoryCustom` → "Personalizzata"
  - `templatePreview.customMeal` → "Personalizzato"
- **"Spoiled"** (it)
  - `pantryAnalytics.reasonSpoiled` → "Andato a male"
  - `recordWaste.reasonSpoiled` → "Andato a male"
  - `addToPantry.conditionSpoiled` → "Andata a male"

## Needs a decision

Either the same English means two different things (a homonym — `Back` is
navigation in one place and the back of a package in another), or it is
genuine synonym drift. Only someone who reads the language can tell.

- **"An unexpected error occurred"** (es)
  - `errors.codes.unexpected` → "Se ha producido un error inesperado"
  - `errors.boundary.message` → "Ocurrió un error inesperado"
  - `errors.unexpected` → "Ocurrió un error inesperado"
- **"This item was updated by another user. Please refresh and try again."** (es)
  - `errors.codes.versionConflict` → "Otro usuario ha actualizado este elemento. Actualiza e inténtalo de nuevo."
  - `errors.entityUpdatedBody` → "Otro usuario actualizó este elemento. Actualiza e inténtalo de nuevo."
- **"Try Again"** (es)
  - `errors.boundary.retry` → "Reintentar"
  - `auth.tryAgain` → "Intentar de nuevo"
  - `onBoarding.tryAgain` → "Reintentar"
- **"Please enter an item name"** (es)
  - `errors.itemNameRequired` → "Introduce el nombre del artículo"
  - `shoppingListScreens.pleaseEnterItemName` → "Introduce un nombre de artículo"
- **"Item"** (es)
  - `errors.entityItem` → "Elemento"
  - `labels.item` → "Artículo"
  - `pantryItemDetail.item` → "Artículo"
- **"Invite"** (es)
  - `errors.entityInvite` → "Invitación"
  - `homeManagement.cardInvite` → "Invitar"
- **"Are you sure you want to remove this member?"** (es)
  - `confirmations.removeMember` → "¿Seguro que quieres quitar este miembro?"
  - `shoppingListScreens.removeMemberMessage` → "¿Seguro que quieres eliminar este miembro?"
- **"Back"** (es)
  - `labels.back` → "Atrás"
  - `itemPhotos.perspective.back` → "Reverso"
- **"Try again"** (es)
  - `labels.tryAgain` → "Intentar de nuevo"
  - `dataState.retry` → "Reintentar"
- **"Shopping List"** (es)
  - `labels.shoppingList` → "Lista de la compra"
  - `shoppingListScreens.listFallback` → "Lista de la compra"
  - `invitationAcceptance.resourceList` → "Lista de la compra"
  - `shoppingListScreen.title` → "Lista de Compras"
  - `onboardingSteps.CreateShoppingList.title` → "Lista de compras"
- **"Clear"** (es)
  - `labels.clear` → "Borrar"
  - `recipeFilters.clear` → "Limpiar"
  - `performance.clear` → "Borrar"
- **"Add to Shopping List"** (es)
  - `recipes.addToShoppingList` → "Añadir a la lista de la compra"
  - `addItemSheet.addToShoppingList` → "Añadir a la lista de compras"
- **"All"** (es)
  - `recipes.filterAll` → "Todas"
  - `notifications.categoryAll` → "Todas"
  - `templateBrowser.categoryAll` → "Todas"
  - `pantryScreen.tabAll` → "Todos"
  - `pantryTabs.all` → "Todos"
- **"Shellfish"** (es)
  - `recipes.intolerance.SHELLFISH` → "Marisco"
  - `recipeFilters.intolerances.shellfish` → "Crustáceos"
  - `dietaryProfile.intolerances.shellfish` → "Crustáceos"
- **"Sulfite"** (es)
  - `recipes.intolerance.SULFITE` → "Sulfito"
  - `recipeFilters.intolerances.sulfite` → "Sulfitos"
  - `dietaryProfile.intolerances.sulfite` → "Sulfitos"
- **"Protein"** (es)
  - `recipes.macroProtein` → "Proteínas"
  - `dietary.protein` → "Proteína"
  - `nutritionSummary.macroProtein` → "Proteínas"
- **"Fat"** (es)
  - `recipes.macroFat` → "Grasas"
  - `dietary.fat` → "Grasa"
  - `nutritionSummary.macroFat` → "Grasas"
- **"Snack"** (es)
  - `recipeFilters.mealTypes.snack` → "Aperitivo"
  - `pantryAnalytics.purposeSnack` → "Tentempié"
  - `addMealSheet.mealSnack` → "Tentempié"
  - `addToMealPlan.mealSnack` → "Tentempié"
  - `consumeItem.purposeSnack` → "Tentempié"
  - `usagePurpose.SNACK` → "Aperitivo"
- **"Condition"** (es)
  - `pantryItemDetail.fields.condition` → "Estado"
  - `addToPantry.condition` → "Condición"
- **"Purchased"** (es)
  - `pantryItemDetail.fields.purchased` → "Comprado"
  - `shoppingListScreens.purchased` → "Comprado"
  - `addToPantry.methodPurchased` → "Comprado"
  - `shoppingListScreen.tabPurchased` → "Comprados"
- **"Daily"** (es)
  - `shoppingListScreens.patternDaily` → "Diariamente"
  - `pantryAnalytics.granularityDaily` → "Diario"
- **"Weekly"** (es)
  - `shoppingListScreens.patternWeekly` → "Semanalmente"
  - `mealPlan.weekly` → "Semanal"
  - `pantryAnalytics.granularityWeekly` → "Semanal"
  - `saveAsTemplate.categoryWeekly` → "Semanal"
- **"Monthly"** (es)
  - `shoppingListScreens.patternMonthly` → "Mensualmente"
  - `mealPlan.monthly` → "Mensual"
  - `pantryAnalytics.granularityMonthly` → "Mensual"
  - `saveAsTemplate.categoryMonthly` → "Mensual"
- **"Active"** (es)
  - `shoppingListScreens.listStatusActive` → "Activa"
  - `shoppingListScreens.statusActive` → "Activo"
  - `mealPlanSelector.filterActive` → "Activos"
- **"Danger Zone"** (es)
  - `shoppingListScreens.dangerZone` → "Zona peligrosa"
  - `pantrySettings.dangerZone` → "Zona peligrosa"
  - `mealPlanSettings.dangerZone` → "Zona de peligro"
  - `homeDetail.sectionDangerZone` → "Zona de Peligro"
- **"e.g., Dairy, Produce"** (es)
  - `shoppingListScreens.categoryPlaceholder` → "p. ej., lácteos, productos frescos"
  - `addToPantry.categoryPlaceholder` → "ej., Lácteos, Frutas"
- **"e.g., 4.99"** (es)
  - `shoppingListScreens.estimatedPricePlaceholder` → "p. ej., 4,99"
  - `addToPantry.costPlaceholder` → "ej., 4,99"
- **"Please enter an email address"** (es)
  - `shoppingListScreens.pleaseEnterEmail` → "Introduce una dirección de email"
  - `inviteUser.emailRequired` → "Por favor, introduce una dirección de correo electrónico"
- **"Cannot Leave"** (es)
  - `shoppingListScreens.cannotLeaveTitle` → "No puedes abandonar"
  - `homeDetail.ownerCannotLeaveTitle` → "No Puedes Salir"
- **"Please enter a valid email address"** (es)
  - `inviteMembers.invalidEmailMessage` → "Introduce una dirección de email válida"
  - `inviteUser.emailInvalid` → "Por favor, introduce una dirección de correo electrónico válida"
  - `commonValidation.emailInvalid` → "Por favor introduce una dirección de correo válida"
  - `validation.email` → "Introduce una dirección de email válida"
- **"Are you sure you want to delete \"{{name}}\"? This cannot be undone."** (es)
  - `storageLocations.deleteConfirm` → "¿Seguro que quieres eliminar \"{{name}}\"? Esto no se puede deshacer."
  - `mealPlanSettings.deleteConfirm` → "¿Seguro que quieres eliminar «{{name}}»? Esta acción no se puede deshacer."
- **"Cooking"** (es)
  - `pantryAnalytics.purposeCooking` → "Cocina"
  - `consumeItem.purposeCooking` → "Cocinar"
  - `usagePurpose.COOKING` → "Cocinado"
- **"Meal Prep"** (es)
  - `pantryAnalytics.purposeMealPrep` → "Preparación"
  - `consumeItem.purposeMealPrep` → "Preparación de comidas"
- **"Restock"** (es)
  - `pantryAnalytics.purposeRestock` → "Reabastecimiento"
  - `restockItem.restock` → "Reponer"
  - `duplicateItem.restock` → "Reponer"
  - `swipeActions.restock` → "Reponer"
- **"Transfer"** (es)
  - `pantryAnalytics.purposeTransfer` → "Transferencia"
  - `consumeItem.purposeTransfer` → "Traslado"
  - `homeDetail.transferOwnershipConfirm` → "Transferir"
  - `usagePurpose.TRANSFER` → "Transferencia"
- **"Cooking Fail"** (es)
  - `pantryAnalytics.reasonCookingFail` → "Fallo al cocinar"
  - `recordWaste.reasonCookingFail` → "Error al cocinar"
- **"Pest"** (es)
  - `pantryAnalytics.reasonPest` → "Plaga"
  - `recordWaste.reasonPest` → "Plagas"
- **"Count"** (es)
  - `performance.count` → "N.º"
  - `baseDimension.COUNT` → "Recuento"
  - `unitType.COUNT` → "Cantidad"
- **"Shopping"** (es)
  - `notifications.categoryShopping` → "Compras"
  - `shoppingListScreen.tabShopping` → "Por comprar"
- **"Success"** (es)
  - `settings.resetSuccessTitle` → "Listo"
  - `invitationAcceptance.successTitle` → "Éxito"
  - `home.joinSuccessTitle` → "Éxito"
- **"Dietary Profile"** (es)
  - `dietary.title` → "Perfil alimentario"
  - `profile.sections.dietaryProfile` → "Perfil alimenticio"
  - `profile.labels.dietaryProfile` → "Perfil alimenticio"
- **"Please Wait"** (es)
  - `loader.pleaseWait` → "Por favor espera"
  - `deduction.pleaseWait` → "Espera"
- **"Create New Folder"** (es)
  - `manageRecipe.createNewFolder` → "Crear nueva carpeta"
  - `saveRecipe.createNewFolder` → "Crear nueva carpeta"
  - `folderPicker.createNew` → "Crear carpeta nueva"
- **"Add tags..."** (es)
  - `manageRecipe.tagsPlaceholder` → "Añadir etiquetas..."
  - `saveRecipe.tagsPlaceholder` → "Añade etiquetas..."
- **"Alert When Below"** (es)
  - `addToPantry.alertWhenBelow` → "Avisar cuando esté por debajo de"
  - `itemForm.alertWhenBelow` → "Avisar cuando baje de"
- **"pcs, dozen"** (es)
  - `addToPantry.unitPlaceholder` → "pcs, docena"
  - `itemForm.placeholderUnit` → "uds, docena"
- **"Enter join code"** (es)
  - `homeManagement.joinCodePlaceholder` → "Introduce el código"
  - `joinHome.codePlaceholder` → "Introduce el código de acceso"
- **"Set Default"** (es)
  - `homeManagement.cardSetDefault` → "Establecer Predeterminado"
  - `storageLocationCard.setDefault` → "Hacer predeterminada"
- **"Leave Home"** (es)
  - `homeDetail.leaveHomeButton` → "Salir del Hogar"
  - `home.leaveTitle` → "Abandonar hogar"
- **"Please enter a join code"** (es)
  - `joinHome.emptyCodeError` → "Introduce un código de acceso"
  - `home.enterJoinCode` → "Introduce un código para unirte"
- **"Mark as purchased"** (es)
  - `shoppingListTutorial.checkbox.title` → "Marca como comprado"
  - `swipeActions.markPurchased` → "Marcar como comprado"
- **"Please enter an item name"** (it)
  - `errors.itemNameRequired` → "Inserisci il nome dell'articolo"
  - `shoppingListScreens.pleaseEnterItemName` → "Inserisci il nome di un articolo"
- **"Item"** (it)
  - `errors.entityItem` → "Elemento"
  - `labels.item` → "Articolo"
  - `pantryItemDetail.item` → "Articolo"
- **"Are you sure you want to delete this item?"** (it)
  - `confirmations.deleteItem` → "Sei sicuro di voler eliminare questo articolo?"
  - `pantryItemDetail.deleteBody` → "Vuoi davvero eliminare questo articolo?"
- **"Back"** (it)
  - `labels.back` → "Indietro"
  - `itemPhotos.perspective.back` → "Retro"
- **"Password must be at least 8 characters"** (it)
  - `auth.passwordTooShort` → "La password deve essere di almeno 8 caratteri"
  - `commonValidation.passwordMin` → "La password deve contenere almeno 8 caratteri"
- **"Nutrition"** (it)
  - `recipes.nutritionTitle` → "Valori nutrizionali"
  - `pantryItemDetail.nutrition` → "Nutrizione"
  - `itemPhotos.perspective.nutrition_label` → "Valori nutrizionali"
- **"All"** (it)
  - `recipes.filterAll` → "Tutte"
  - `notifications.categoryAll` → "Tutte"
  - `templateBrowser.categoryAll` → "Tutti"
  - `pantryScreen.tabAll` → "Tutti"
  - `pantryTabs.all` → "Tutti"
- **"{{count}} item"** (it)
  - `recipes.listItemCount_one` → "{{count}} elemento"
  - `storageLocations.itemSingular` → "{{count}} articolo"
  - `pantryScreen.itemCount_one` → "{{count}} articolo"
  - `storageLocationCard.itemCount_one` → "{{count}} articolo"
- **"{{count}} items"** (it)
  - `recipes.listItemCount_other` → "{{count}} elementi"
  - `pantrySettings.itemsCount` → "{{count}} articoli"
  - `storageLocations.itemPlural` → "{{count}} articoli"
  - `generateShoppingList.itemsCount` → "{{count}} articoli"
  - `pantryScreen.itemCount_other` → "{{count}} articoli"
  - `storageLocationCard.itemCount_other` → "{{count}} articoli"
- **"Low Sodium"** (it)
  - `recipes.healthGoal.LOW_SODIUM` → "Povero di sodio"
  - `dietaryProfile.goals.lowSodium` → "Basso contenuto di sodio"
- **"Sulfite"** (it)
  - `recipes.intolerance.SULFITE` → "Solfito"
  - `recipeFilters.intolerances.sulfite` → "Solfiti"
  - `dietaryProfile.intolerances.sulfite` → "Solfiti"
- **"Max Cook Time"** (it)
  - `recipeFilters.maxCookTimeTitle` → "Tempo max di cottura"
  - `dietary.maxCookTime` → "Tempo max cottura"
- **"Low FODMAP"** (it)
  - `recipeFilters.diets.lowFodmap` → "Low FODMAP"
  - `dietaryProfile.diets.lowFodmap` → "Basso FODMAP"
- **"Dessert"** (it)
  - `recipeFilters.mealTypes.dessert` → "Dessert"
  - `addMealSheet.mealDessert` → "Dolce"
  - `addToMealPlan.mealDessert` → "Dolce"
- **"Purchased"** (it)
  - `pantryItemDetail.fields.purchased` → "Acquistato"
  - `shoppingListScreens.purchased` → "Acquistato"
  - `addToPantry.methodPurchased` → "Acquistato"
  - `shoppingListScreen.tabPurchased` → "Acquistati"
- **"Daily"** (it)
  - `shoppingListScreens.patternDaily` → "Ogni giorno"
  - `pantryAnalytics.granularityDaily` → "Giornaliero"
- **"Weekly"** (it)
  - `shoppingListScreens.patternWeekly` → "Ogni settimana"
  - `mealPlan.weekly` → "Settimanale"
  - `pantryAnalytics.granularityWeekly` → "Settimanale"
  - `saveAsTemplate.categoryWeekly` → "Settimanale"
- **"Monthly"** (it)
  - `shoppingListScreens.patternMonthly` → "Ogni mese"
  - `mealPlan.monthly` → "Mensile"
  - `pantryAnalytics.granularityMonthly` → "Mensile"
  - `saveAsTemplate.categoryMonthly` → "Mensile"
- **"Active"** (it)
  - `shoppingListScreens.listStatusActive` → "Attiva"
  - `shoppingListScreens.statusActive` → "Attivo"
  - `mealPlanSelector.filterActive` → "Attivi"
- **"Danger Zone"** (it)
  - `shoppingListScreens.dangerZone` → "Zona di pericolo"
  - `pantrySettings.dangerZone` → "Zona di pericolo"
  - `mealPlanSettings.dangerZone` → "Zona pericolo"
  - `homeDetail.sectionDangerZone` → "Zona Pericolosa"
- **"Cannot Leave"** (it)
  - `shoppingListScreens.cannotLeaveTitle` → "Non puoi abbandonare"
  - `homeDetail.ownerCannotLeaveTitle` → "Impossibile Lasciare"
- **"Please select a unit for the net weight."** (it)
  - `shoppingListScreens.netWeightUnitRequired` → "Seleziona un’unità per il peso netto."
  - `addToPantry.netWeightUnitRequired` → "Seleziona un'unità per il peso netto."
- **"Create Meal Plan"** (it)
  - `mealPlan.createTitle` → "Crea piano dei pasti"
  - `templatePreview.createMealPlan` → "Crea piano pasti"
- **"Pantry Name"** (it)
  - `pantrySettings.name` → "Nome dispensa"
  - `onBoarding.pantryNameLabel` → "Nome della dispensa"
- **"Description (Optional)"** (it)
  - `pantrySettings.descriptionLabel` → "Descrizione (opzionale)"
  - `storageLocationForm.descriptionLabel` → "Descrizione (facoltativa)"
- **"Please enter a valid email address"** (it)
  - `inviteMembers.invalidEmailMessage` → "Inserisci un'email valida"
  - `inviteUser.emailInvalid` → "Inserisci un indirizzo email valido"
  - `commonValidation.emailInvalid` → "Inserisci un indirizzo email valido"
  - `validation.email` → "Inserisci un indirizzo email valido"
- **"Storage Locations"** (it)
  - `storageLocations.title` → "Posizioni di archiviazione"
  - `homeDetail.storageLocationsTitle` → "Posizioni di Conservazione"
- **"Are you sure you want to delete \"{{name}}\"? This cannot be undone."** (it)
  - `storageLocations.deleteConfirm` → "Sei sicuro di voler eliminare \"{{name}}\"? Non può essere annullato."
  - `mealPlanSettings.deleteConfirm` → "Sei sicuro di voler eliminare «{{name}}»? Questa azione non può essere annullata."
- **"Usage"** (it)
  - `pantryAnalytics.tabUsage` → "Utilizzo"
  - `performance.usage` → "Uso"
- **"Waste"** (it)
  - `pantryAnalytics.tabWaste` → "Sprechi"
  - `pantryAnalytics.purposeWaste` → "Spreco"
- **"Cooking"** (it)
  - `pantryAnalytics.purposeCooking` → "Cucina"
  - `consumeItem.purposeCooking` → "Cucina"
  - `usagePurpose.COOKING` → "Cucinato"
- **"Restock"** (it)
  - `pantryAnalytics.purposeRestock` → "Riassortimento"
  - `restockItem.restock` → "Rifornisci"
  - `duplicateItem.restock` → "Rifornisci"
  - `swipeActions.restock` → "Rifornisci"
- **"Transfer"** (it)
  - `pantryAnalytics.purposeTransfer` → "Trasferimento"
  - `consumeItem.purposeTransfer` → "Trasferimento"
  - `homeDetail.transferOwnershipConfirm` → "Trasferisci"
  - `usagePurpose.TRANSFER` → "Trasferimento"
- **"Cooking Fail"** (it)
  - `pantryAnalytics.reasonCookingFail` → "Errore di cottura"
  - `recordWaste.reasonCookingFail` → "Cottura fallita"
- **"Overstock"** (it)
  - `pantryAnalytics.reasonOverstock` → "Eccesso di stock"
  - `recordWaste.reasonOverstock` → "Eccesso di scorte"
- **"Performance Dashboard"** (it)
  - `performance.title` → "Dashboard prestazioni"
  - `profile.labels.performanceDashboard` → "Pannello prestazioni"
- **"Count"** (it)
  - `performance.count` → "Conteggio"
  - `baseDimension.COUNT` → "Conteggio"
  - `unitType.COUNT` → "Quantità"
- **"Shopping"** (it)
  - `notifications.categoryShopping` → "Spesa"
  - `shoppingListScreen.tabShopping` → "Da comprare"
- **"Success"** (it)
  - `settings.resetSuccessTitle` → "Fatto"
  - `invitationAcceptance.successTitle` → "Successo"
  - `home.joinSuccessTitle` → "Fatto"
- **"You're all set!"** (it)
  - `onBoarding.allSet` → "Tutto pronto!"
  - `onboardingSteps.OnboardingComplete.subtitle` → "È tutto pronto!"
- **"Profile Picture"** (it)
  - `onBoarding.profilePictureTitle` → "Foto del profilo"
  - `onboardingSteps.ProfilePictureUpload.title` → "Foto profilo"
- **"Home Name"** (it)
  - `onBoarding.homeNameLabel` → "Nome della casa"
  - `homeDetail.labelHomeName` → "Nome Casa"
- **"Please Wait"** (it)
  - `loader.pleaseWait` → "Attendere prego"
  - `deduction.pleaseWait` → "Attendi"
- **"Ambient"** (it)
  - `storageLocationForm.tempAmbient` → "Ambiente"
  - `moveToPantry.stateAmbient` → "Ambiente"
  - `storageState.AMBIENT` → "Temperatura ambiente"
- **"Notes (Optional)"** (it)
  - `editCustomMeal.notesLabel` → "Note (facoltative)"
  - `markCookedModal.notesOptional` → "Note (opzionale)"
  - `moveToPantry.notesOptional` → "Note (opzionale)"
- **"Enter folder name..."** (it)
  - `manageRecipe.enterFolderName` → "Inserisci il nome della cartella..."
  - `saveRecipe.enterFolderName` → "Inserisci nome cartella..."
  - `folderPicker.namePlaceholder` → "Inserisci il nome della cartella..."
- **"Name must be at least 2 characters"** (it)
  - `addStorageLocation.nameTooShort` → "Il nome deve avere almeno 2 caratteri"
  - `commonValidation.nameMin` → "Il nome deve contenere almeno 2 caratteri"
- **"Restock To"** (it)
  - `addToPantry.restockTo` → "Rifornisci fino a"
  - `itemForm.restockTo` → "Rifornire fino a"
- **"pcs, dozen"** (it)
  - `addToPantry.unitPlaceholder` → "pcs, dozzina"
  - `itemForm.placeholderUnit` → "pz, dozzina"
- **"Net weight is used for consumption tracking and is optional."** (it)
  - `addToPantry.netWeightHint` → "Il peso netto viene usato per monitorare il consumo ed è opzionale."
  - `itemForm.netWeightHelp` → "Il peso netto serve per tracciare il consumo ed è facoltativo."
- **"Debug Info"** (it)
  - `profile.labels.debugInfo` → "Informazioni di debug"
  - `debugInfo.title` → "Info di debug"
- **"Pantry settings"** (it)
  - `pantryScreen.settingsAccessibility` → "Impostazioni dispensa"
  - `pantryScreen.tutorialPantrySettingsTitle` → "Impostazioni della dispensa"
- **"Fridge"** (it)
  - `pantryScreen.tabFridge` → "Frigorifero"
  - `pantryTabs.fridge` → "Frigorifero"
  - `storageStateShort.REFRIGERATED` → "Frigo"
- **"Enter join code"** (it)
  - `homeManagement.joinCodePlaceholder` → "Inserisci codice di accesso"
  - `joinHome.codePlaceholder` → "Inserisci il codice di accesso"
- **"Set Default"** (it)
  - `homeManagement.cardSetDefault` → "Imposta Predefinita"
  - `storageLocationCard.setDefault` → "Imposta come predefinita"
- **"Enter home name"** (it)
  - `homeDetail.placeholderHomeName` → "Inserisci nome della casa"
  - `createHome.namePlaceholder` → "Inserisci il nome della casa"
- **"Something went wrong. Please try again."** (sq)
  - `errors.codes.genericRetry` → "Diçka shkoi keq. Provoni sërish."
  - `errors.somethingWentWrong` → "Diçka shkoi keq. Provoje sërish."
- **"This item was updated by another user. Please refresh and try again."** (sq)
  - `errors.codes.versionConflict` → "Ky artikull u përditësua nga një përdorues tjetër. Rifreskoni dhe provoni sërish."
  - `errors.entityUpdatedBody` → "Ky artikull u përditësua nga një përdorues tjetër. Rifreskoni dhe provoni përsëri."
- **"Not available offline"** (sq)
  - `errors.notAvailableOffline` → "Nuk është i disponueshëm pa internet"
  - `dataState.offlineTitle` → "E padisponueshme pa internet"
- **"Try Again"** (sq)
  - `errors.boundary.retry` → "Provo përsëri"
  - `auth.tryAgain` → "Provo përsëri"
  - `onBoarding.tryAgain` → "Provo sërish"
- **"Please enter an item name"** (sq)
  - `errors.itemNameRequired` → "Ju lutemi shkruani emrin e artikullit"
  - `shoppingListScreens.pleaseEnterItemName` → "Shkruaj emrin e një artikulli"
- **"Please enter a valid quantity"** (sq)
  - `errors.invalidQuantity` → "Ju lutemi shkruani një sasi të vlefshme"
  - `adjustQuantity.invalidQuantity` → "Shkruaj një sasi të vlefshme"
  - `moveToPantry.invalidQuantity` → "Shkruaj një sasi të vlefshme"
  - `deduction.invalidQuantity` → "Shkruaj një sasi të vlefshme"
- **"Home"** (sq)
  - `errors.entityHome` → "Shtëpia"
  - `labels.home` → "Shtëpia"
  - `notifications.categoryHome` → "Shtëpia"
  - `invitationAcceptance.resourceHome` → "Shtëpia"
  - `homeManagement.statsHome_one` → "Shtëpi"
  - `joinHome.homeFallback` → "Shtëpia"
- **"Invite"** (sq)
  - `errors.entityInvite` → "Ftesë"
  - `homeManagement.cardInvite` → "Fto"
- **"Edit"** (sq)
  - `labels.edit` → "Modifiko"
  - `pantrySelector.editSelectedPantry` → "Modifiko"
  - `suggestItemEdit.editAction` → "Ndrysho"
  - `swipeActions.edit` → "Modifiko"
- **"Back"** (sq)
  - `labels.back` → "Mbrapa"
  - `itemPhotos.perspective.back` → "Prapa"
- **"Done"** (sq)
  - `labels.done` → "U bë"
  - `mealPlanSettings.done` → "U krye"
- **"Pantry"** (sq)
  - `labels.pantry` → "Qilari"
  - `notifications.categoryPantry` → "Qilari"
  - `navigation.tabs.pantry` → "Qilari"
  - `pantryScreen.tabPantry` → "Qilar"
  - `homeManagement.statsPantry_one` → "Qilar"
  - `pantryTabs.pantry` → "Qilar"
- **"Default"** (sq)
  - `labels.default` → "I parazgjedhur"
  - `recipes.defaultBadge` → "Parazgjedhur"
  - `appearance.colorDefault` → "I parazgjedhur"
  - `appearance.fontDefault` → "Të parazgjedhura"
  - `moveToPantry.defaultLabel` → "I parazgjedhur"
  - `homeManagement.cardDefault` → "Parazgjedhur"
  - `storageLocationCard.default` → "Parazgjedhur"
- **"Shopping List"** (sq)
  - `labels.shoppingList` → "Lista e blerjeve"
  - `shoppingListScreens.listFallback` → "Listë blerjesh"
  - `invitationAcceptance.resourceList` → "Listë blerjesh"
  - `shoppingListScreen.title` → "Lista e Blerjeve"
  - `onboardingSteps.CreateShoppingList.title` → "Lista e blerjeve"
- **"Tap to view details"** (sq)
  - `labels.tapToViewDetails` → "Trokit për të parë detajet"
  - `a11y.tapToViewDetails` → "Prek për të parë detajet"
- **"Loading"** (sq)
  - `labels.loading` → "Po ngarkohet"
  - `pantryItemDetail.loading` → "Po ngarkohet"
  - `loader.loading` → "Po ngarkohet"
  - `invitationAcceptance.loadingMessage` → "Duke ngarkuar"
  - `homeManagement.loadingMessage` → "Po ngarkohet"
  - `homeDetail.loadingMessage` → "Po ngarkohet"
- **"Tap to edit"** (sq)
  - `instructions.tapToEdit` → "Trokit për ta modifikuar"
  - `settingRow.tapToEdit` → "Trokit për të modifikuar"
- **"Name"** (sq)
  - `auth.name` → "Emri"
  - `mealPlan.name` → "Emri"
  - `storageLocationForm.name` → "Emri"
  - `pantrySort.sortName` → "Emrit"
- **"Description"** (sq)
  - `recipes.description` → "Përshkrim"
  - `mealPlan.description` → "Përshkrim"
  - `saveAsTemplate.descriptionLabel` → "Përshkrimi"
  - `mealTemplateBuilder.description` → "Përshkrimi"
  - `addItemForm.fields.description.label` → "Përshkrimi"
- **"Servings"** (sq)
  - `recipes.servings` → "Pjesë"
  - `templatePreview.servingsLabel` → "Racione"
  - `mealTemplateBuilder.servings` → "Racione"
- **"Health Goals"** (sq)
  - `recipes.healthGoals` → "Synimet shëndetësore"
  - `dietaryProfile.goalsTitle` → "Synime shëndetësore"
- **"Quantity"** (sq)
  - `recipes.quantity` → "Sasia"
  - `pantryItemDetail.fields.quantity` → "Sasia"
  - `shoppingListScreens.quantity` → "Sasia"
  - `pantrySort.sortQuantity` → "Sasisë"
  - `moveToPantry.quantity` → "Sasia"
  - `quantityEditSheet.quantity` → "Sasia"
  - `purchaseAmountSheet.quantity` → "Sasia"
  - `addToPantry.quantity` → "Sasia"
  - `a11y.quantityLabel` → "Sasia"
- **"Notes"** (sq)
  - `recipes.notes` → "Shënime"
  - `pantryItemDetail.notes` → "Shënime"
  - `shoppingListScreens.notes` → "Shënime"
  - `manageRecipe.notes` → "Shënimet"
  - `recordWaste.notes` → "Shënime"
  - `restockItem.notes` → "Shënime"
  - `consumeItem.notes` → "Shënime"
  - `addToPantry.notes` → "Shënime"
  - `itemForm.notes` → "Shënime"
- **"Optional"** (sq)
  - `recipes.optional` → "Opsionale"
  - `ingredientMatch.optional` → "Opsional"
- **"Tags"** (sq)
  - `recipes.tags` → "Etiketa"
  - `recipes.recipeTags` → "Etiketa"
  - `pantryItemDetail.tags` → "Etiketa"
  - `manageRecipe.tags` → "Etiketat"
  - `mealTemplateBuilder.tags` → "Etiketa"
  - `addToPantry.tags` → "Etiketat"
  - `addItemForm.fields.tags.label` → "Etiketa"
  - `itemForm.tags` → "Etiketat"
- **"Nutrition"** (sq)
  - `recipes.nutritionTitle` → "Ushqyerja"
  - `pantryItemDetail.nutrition` → "Ushqyerja"
  - `itemPhotos.perspective.nutrition_label` → "Ushqyesit"
- **"Failed to update recipe"** (sq)
  - `recipes.publishFailed` → "Receta nuk u përditësua"
  - `recipes.updateRecipeMetaFailed` → "Përditësimi i recetës dështoi"
- **"Vegetarian"** (sq)
  - `recipes.vegetarian` → "Vegjetariane"
  - `recipes.diet.VEGETARIAN` → "Vegjetarian"
  - `recipeFilters.diets.vegetarian` → "Vegjetariane"
  - `addMealSheet.dietVegetarian` → "Vegjetarian"
  - `dietaryProfile.diets.vegetarian` → "Vegjetariane"
- **"Vegan"** (sq)
  - `recipes.vegan` → "Vegane"
  - `recipes.diet.VEGAN` → "Vegan"
  - `recipeFilters.diets.vegan` → "Vegan"
  - `addMealSheet.dietVegan` → "Vegan"
  - `dietaryProfile.diets.vegan` → "Vegane"
- **"Lacto Vegetarian"** (sq)
  - `recipes.diet.LACTO_VEGETARIAN` → "Lakto-vegjetarian"
  - `dietaryProfile.diets.lactoVegetarian` → "Lakto-vegjetariane"
- **"Ovo Vegetarian"** (sq)
  - `recipes.diet.OVO_VEGETARIAN` → "Ovo-vegjetarian"
  - `dietaryProfile.diets.ovoVegetarian` → "Ovo-vegjetariane"
- **"Pescetarian"** (sq)
  - `recipes.diet.PESCETARIAN` → "Peshkatar"
  - `recipeFilters.diets.pescetarian` → "Pescetariane"
  - `dietaryProfile.diets.pescetarian` → "Peshkatariane"
- **"Primal"** (sq)
  - `recipes.diet.PRIMAL` → "Primal"
  - `recipeFilters.diets.primal` → "Primal"
  - `dietaryProfile.diets.primal` → "Primale"
- **"High Protein"** (sq)
  - `recipes.healthGoal.HIGH_PROTEIN` → "I lartë në proteina"
  - `dietaryProfile.goals.highProtein` → "I pasur me proteina"
  - `nutritionHighlights.highProtein` → "Shumë proteina"
- **"Low Carb"** (sq)
  - `recipes.healthGoal.LOW_CARB` → "I ulët në karbohidrate"
  - `dietaryProfile.goals.lowCarb` → "Pak karbohidrate"
- **"Low Sodium"** (sq)
  - `recipes.healthGoal.LOW_SODIUM` → "I ulët në natrium"
  - `dietaryProfile.goals.lowSodium` → "Pak natrium"
- **"Seafood"** (sq)
  - `recipes.intolerance.SEAFOOD` → "Ushqim deti"
  - `recipeFilters.intolerances.seafood` → "Fruta deti"
  - `dietaryProfile.intolerances.seafood` → "Fruta deti"
- **"Shellfish"** (sq)
  - `recipes.intolerance.SHELLFISH` → "Guaskorë"
  - `recipeFilters.intolerances.shellfish` → "Butakë"
  - `dietaryProfile.intolerances.shellfish` → "Guaskorë"
- **"Sulfite"** (sq)
  - `recipes.intolerance.SULFITE` → "Sulfit"
  - `recipeFilters.intolerances.sulfite` → "Sulfite"
  - `dietaryProfile.intolerances.sulfite` → "Sulfite"
- **"Protein"** (sq)
  - `recipes.macroProtein` → "Proteina"
  - `dietary.protein` → "Proteinë"
  - `nutritionSummary.macroProtein` → "Proteina"
- **"Fat"** (sq)
  - `recipes.macroFat` → "Yndyra"
  - `dietary.fat` → "Yndyrë"
  - `nutritionSummary.macroFat` → "Yndyrë"
- **"Max Cook Time"** (sq)
  - `recipeFilters.maxCookTimeTitle` → "Koha max e gatimit"
  - `dietary.maxCookTime` → "Koha maks. e gatimit"
- **"Snack"** (sq)
  - `recipeFilters.mealTypes.snack` → "Ushqim i shpejtë"
  - `pantryAnalytics.purposeSnack` → "Snak"
  - `addMealSheet.mealSnack` → "Meze"
  - `addToMealPlan.mealSnack` → "Meze"
  - `consumeItem.purposeSnack` → "Snack"
  - `usagePurpose.SNACK` → "Meze"
- **"Purchased"** (sq)
  - `pantryItemDetail.fields.purchased` → "Blerë"
  - `shoppingListScreens.purchased` → "Blerë"
  - `addToPantry.methodPurchased` → "Blerë"
  - `shoppingListScreen.tabPurchased` → "Të blera"
- **"Added"** (sq)
  - `pantryItemDetail.fields.added` → "Shtuar"
  - `pantryAnalytics.added` → "Të shtuar"
  - `barcode.added` → "U shtua"
- **"Wasted"** (sq)
  - `pantryItemDetail.batch.wasted` → "Shpenzuar"
  - `pantryAnalytics.wasted` → "Të humbur"
  - `usagePurpose.WASTE` → "Hedhur"
- **"Expired"** (sq)
  - `pantryItemDetail.batch.expired` → "Skaduar"
  - `shoppingListScreens.statusExpired` → "I skaduar"
  - `pantryAnalytics.reasonExpired` → "I skaduar"
  - `filteredPantry.expired` → "Skaduar"
  - `recordWaste.reasonExpired` → "Skaduar"
  - `addToPantry.conditionExpired` → "Skaduar"
  - `expiration.expired` → "Skaduar"
- **"Are you sure you want to delete this list? This action cannot be undone."** (sq)
  - `shoppingListScreens.deleteListConfirmMessage` → "Je i sigurt që do ta fshish këtë listë? Ky veprim nuk mund të zhbëhet."
  - `shoppingListSelector.deleteAlertMessage_one` → "Je i sigurt që dëshiron ta fshish këtë listë? Ky veprim nuk mund të zhbëhet."
- **"Daily"** (sq)
  - `shoppingListScreens.patternDaily` → "Çdo ditë"
  - `pantryAnalytics.granularityDaily` → "Ditore"
- **"Weekly"** (sq)
  - `shoppingListScreens.patternWeekly` → "Çdo javë"
  - `mealPlan.weekly` → "Javor"
  - `pantryAnalytics.granularityWeekly` → "Javore"
  - `saveAsTemplate.categoryWeekly` → "Javore"
- **"Monthly"** (sq)
  - `shoppingListScreens.patternMonthly` → "Çdo muaj"
  - `mealPlan.monthly` → "Mujor"
  - `pantryAnalytics.granularityMonthly` → "Mujore"
  - `saveAsTemplate.categoryMonthly` → "Mujore"
- **"Custom"** (sq)
  - `shoppingListScreens.patternCustom` → "E personalizuar"
  - `storageLocationForm.typeCustom` → "I personalizuar"
  - `saveAsTemplate.categoryCustom` → "E personalizuar"
  - `templatePreview.customMeal` → "I personalizuar"
- **"Active"** (sq)
  - `shoppingListScreens.listStatusActive` → "Aktive"
  - `shoppingListScreens.statusActive` → "Aktiv"
  - `mealPlanSelector.filterActive` → "Aktivë"
- **"Template"** (sq)
  - `shoppingListScreens.listStatusTemplate` → "Model"
  - `mealPlanSelector.createFromTemplate` → "Modeli"
- **"Owner"** (sq)
  - `shoppingListScreens.owner` → "Pronari"
  - `inviteUser.roleOwnerLabel` → "Pronar"
  - `roles.owner` → "Pronar"
  - `collaboratorRoles.owner` → "Pronar"
  - `homeRoles.owner` → "Pronar"
- **"Danger Zone"** (sq)
  - `shoppingListScreens.dangerZone` → "Zona e rrezikut"
  - `pantrySettings.dangerZone` → "Zona e rrezikut"
  - `mealPlanSettings.dangerZone` → "Zona e rrezikut"
  - `homeDetail.sectionDangerZone` → "Zona e Rrezikshme"
- **"e.g., Dairy, Produce"** (sq)
  - `shoppingListScreens.categoryPlaceholder` → "p.sh. bulmet, perime"
  - `addToPantry.categoryPlaceholder` → "p.sh., Bulmet, Fruta"
- **"Please enter an email address"** (sq)
  - `shoppingListScreens.pleaseEnterEmail` → "Shkruaj një adresë email"
  - `inviteUser.emailRequired` → "Të lutem fut një adresë email"
- **"Create Meal Plan"** (sq)
  - `mealPlan.createTitle` → "Krijo plan vakti"
  - `templatePreview.createMealPlan` → "Krijo plan vaktesh"
- **"Default Servings"** (sq)
  - `mealPlan.defaultServings` → "Pjesë të parazgjedhura"
  - `mealTemplateBuilder.defaultServings` → "Racionet e parazgjedhura"
- **"Personal"** (sq)
  - `mealPlan.personal` → "Personale"
  - `mealPlanSelector.personalSubtitle` → "Personal"
- **"Description (Optional)"** (sq)
  - `pantrySettings.descriptionLabel` → "Përshkrim (opsional)"
  - `storageLocationForm.descriptionLabel` → "Përshkrimi (opsional)"
- **"Please enter a valid email address"** (sq)
  - `inviteMembers.invalidEmailMessage` → "Shkruaj një email të vlefshëm"
  - `inviteUser.emailInvalid` → "Të lutem fut një adresë email të vlefshme"
  - `commonValidation.emailInvalid` → "Të lutemi shkruaj një adresë emaili të vlefshme"
  - `validation.email` → "Shkruaj një adresë emaili të vlefshme"
- **"Storage Locations"** (sq)
  - `storageLocations.title` → "Vendet e ruajtjes"
  - `homeDetail.storageLocationsTitle` → "Vendndodhjet e Ruajtjes"
- **"Add Location"** (sq)
  - `storageLocations.addLocation` → "Shto vend"
  - `addStorageLocation.addLocation` → "Shto vendndodhje"
- **"Are you sure you want to delete \"{{name}}\"? This cannot be undone."** (sq)
  - `storageLocations.deleteConfirm` → "Je i sigurt që do ta fshish \"{{name}}\"? Ky veprim nuk mund të zhbëhet."
  - `mealPlanSettings.deleteConfirm` → "Je i sigurt që dëshiron të fshish «{{name}}»? Ky veprim nuk mund të zhbëhet."
- **"Waste"** (sq)
  - `pantryAnalytics.tabWaste` → "Mbeturinat"
  - `pantryAnalytics.purposeWaste` → "Mbeturinë"
- **"Composted"** (sq)
  - `pantryAnalytics.composted` → "Kompostuar"
  - `recordWaste.composted` → "Të kompostuara"
- **"Consumed"** (sq)
  - `pantryAnalytics.consumed` → "Të konsumuar"
  - `expirationAction.consumed` → "Konsumuar"
  - `usagePurpose.GENERAL` → "Konsumuar"
- **"Cooking"** (sq)
  - `pantryAnalytics.purposeCooking` → "Gatim"
  - `consumeItem.purposeCooking` → "Gatim"
  - `usagePurpose.COOKING` → "Gatuar"
- **"Restock"** (sq)
  - `pantryAnalytics.purposeRestock` → "Riblerje"
  - `restockItem.restock` → "Riplotëso"
  - `duplicateItem.restock` → "Riplotëso"
  - `swipeActions.restock` → "Riplotëso"
- **"Transfer"** (sq)
  - `pantryAnalytics.purposeTransfer` → "Transferim"
  - `consumeItem.purposeTransfer` → "Transferim"
  - `homeDetail.transferOwnershipConfirm` → "Transfero"
  - `usagePurpose.TRANSFER` → "Transferim"
- **"Burnt"** (sq)
  - `pantryAnalytics.reasonBurnt` → "I djegur"
  - `recordWaste.reasonBurnt` → "Djegur"
- **"Overstock"** (sq)
  - `pantryAnalytics.reasonOverstock` → "Tepricë"
  - `recordWaste.reasonOverstock` → "Stok i tepërt"
- **"Pest"** (sq)
  - `pantryAnalytics.reasonPest` → "Insekte"
  - `recordWaste.reasonPest` → "Dëmtues"
- **"Spilled"** (sq)
  - `pantryAnalytics.reasonSpilled` → "I derdhur"
  - `recordWaste.reasonSpilled` → "Derdhur"
- **"Spoiled"** (sq)
  - `pantryAnalytics.reasonSpoiled` → "I prishur"
  - `recordWaste.reasonSpoiled` → "Prishur"
  - `addToPantry.conditionSpoiled` → "Prishur"
- **"Count"** (sq)
  - `performance.count` → "Numri"
  - `baseDimension.COUNT` → "Numër"
  - `unitType.COUNT` → "Sasi"
- **"Shopping"** (sq)
  - `notifications.categoryShopping` → "Blerjet"
  - `shoppingListScreen.tabShopping` → "Për të blerë"
- **"Success"** (sq)
  - `settings.resetSuccessTitle` → "U bë"
  - `invitationAcceptance.successTitle` → "Sukses"
  - `home.joinSuccessTitle` → "Sukses"
- **"Create List"** (sq)
  - `onBoarding.createList` → "Krijo listën"
  - `addItemSheet.createList` → "Krijo listë"
  - `shoppingListScreen.noListsAction` → "Krijo Listë"
- **"Invalid Image"** (sq)
  - `onBoarding.invalidImageTitle` → "Imazh i pavlefshëm"
  - `profile.invalidImage` → "Imazh i pavlefshëm"
  - `imagePicker.invalidImageTitle` → "Foto e pavlefshme"
- **"Please Wait"** (sq)
  - `loader.pleaseWait` → "Ju lutemi prisni"
  - `deduction.pleaseWait` → "Prit pak"
- **"Guest"** (sq)
  - `inviteUser.roleGuestLabel` → "Mysafir"
  - `roles.guest` → "I ftuar"
  - `homeRoles.guest` → "I ftuar"
- **"Freezer"** (sq)
  - `storageLocationForm.typeFreezer` → "Ngrirës"
  - `pantryScreen.tabFreezer` → "Ngrirës"
  - `pantryTabs.freezer` → "Ngrirës"
  - `storageStateShort.FROZEN` → "Ngrirje"
- **"Ambient"** (sq)
  - `storageLocationForm.tempAmbient` → "Mjedis"
  - `moveToPantry.stateAmbient` → "Ambient"
  - `storageState.AMBIENT` → "Ambient"
- **"Refrigerated"** (sq)
  - `storageLocationForm.tempRefrigerated` → "I ftohur"
  - `moveToPantry.stateRefrigerated` → "Në frigorifer"
  - `storageState.REFRIGERATED` → "Në frigorifer"
- **"Meal Plan"** (sq)
  - `navigation.tabs.mealPlan` → "Plani"
  - `addToMealPlan.mealPlanLabel` → "Plani i vakteve"
  - `mealPlanMain.defaultTitle` → "Plani i vakteve"
- **"Fat (g)"** (sq)
  - `nutritionGoal.labelFatG` → "Yndyrë (g)"
  - `macroTargets.fat` → "Yndyrna (g)"
- **"Enter folder name..."** (sq)
  - `manageRecipe.enterFolderName` → "Fut emrin e dosjes..."
  - `saveRecipe.enterFolderName` → "Shkruaj emrin e dosjes..."
  - `folderPicker.namePlaceholder` → "Shkruaj emrin e dosjes..."
- **"Type to search units..."** (sq)
  - `quantityEditSheet.typeToSearchUnits` → "Shkruaj për të kërkuar njësitë..."
  - `autocomplete.unitSearch` → "Shkruaj për të kërkuar njësi..."
- **"Alert When Below"** (sq)
  - `addToPantry.alertWhenBelow` → "Sinjalizo kur është nën"
  - `itemForm.alertWhenBelow` → "Njofto kur nën"
- **"Restock To"** (sq)
  - `addToPantry.restockTo` → "Riplotëso deri në"
  - `itemForm.restockTo` → "Rimbush deri në"
- **"pcs, dozen"** (sq)
  - `addToPantry.unitPlaceholder` → "pcs, duzinë"
  - `itemForm.placeholderUnit` → "copë, duzinë"
- **"Net weight is used for consumption tracking and is optional."** (sq)
  - `addToPantry.netWeightHint` → "Pesha neto përdoret për gjurmimin e konsumit dhe është opsionale."
  - `itemForm.netWeightHelp` → "Pesha neto përdoret për ndjekjen e konsumit dhe është opsionale."
- **"Create Home"** (sq)
  - `homeManagement.modeCreate` → "Krijo Shtëpi"
  - `onboardingSteps.CreateHome.title` → "Krijo shtëpinë"
- **"Enter join code"** (sq)
  - `homeManagement.joinCodePlaceholder` → "Shkruaj kodin e bashkimit"
  - `joinHome.codePlaceholder` → "Fut kodin e bashkimit"
- **"Members"** (sq)
  - `homeManagement.statsMember_other` → "Anëtarë"
  - `homeManagement.cardMembersSectionTitle` → "Anëtarët"
- **"Please enter a join code"** (sq)
  - `joinHome.emptyCodeError` → "Të lutemi fut një kod bashkimi"
  - `home.enterJoinCode` → "Fut një kod bashkimi"
- **"UPC/Barcode"** (sq)
  - `addItemForm.fields.upc.label` → "UPC/Barkodi"
  - `barcode.upc` → "UPC/Barkod"
- **"Weight"** (sq)
  - `netWeightEntry.weightLabel` → "Pesha"
  - `unitType.WEIGHT` → "Peshë"
