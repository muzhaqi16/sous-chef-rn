# `sous-chef/no-rendered-server-message`

A server error's `message`, or other copy the server writes, never reaches the screen.

## Reports

A `message` read (`x.message`, `x?.message`, `const { message } = x`) whose object is typed by the server:

- a type declared in `src/graphql/generated/**` or a `*.generated.ts` file (`DeletionBlocker`, `Notification`, an `Error` implementer, an operation result);
- an Apollo or GraphQL error: `CombinedGraphQLErrors`, `CombinedProtocolErrors`, `ServerError`, `ServerParseError`, `GraphQLError`, `GraphQLFormattedError`, `ErrorLike`.

The same read of a field in [the server-copy table](#server-copy-fields) (`SERVER_COPY_FIELDS` in the source): the field is declared in a generated file and the object's `__typename` is the row's type, so a fragment, an operation result and the schema type all match.

It reports only when the value reaches output: a JSX child, a JSX attribute value (`key` aside), an argument to `toastService.*` / `alertService.*`, or an interpolation value passed to `t(…)`. The read is followed through `?.`, `!`, `||` / `??`, either branch of a `?:`, the right side of `&&`, a template literal, `+`, `String(…)`, `trim()`-style string methods, an object or array literal, and a `const` it is assigned to.

## Options

- `followProjections` (default `false`; on in `eslint/project.js`) — also follow a read that is stored, through the same operators, as a property of an object literal that does not itself reach output (`.map(n => ({ heading: n.title }))`, `return { body: b.message }`, a setter's argument). The read is reported as `renderedServerCopyProjected` / `renderedServerMessageProjected` when the same file renders a read of that property: `row.heading`, or a destructured `{ heading }` binding (a parameter included), whose property resolves to the literal's own property or to the app-declared property the literal is typed by (`function project(b): Row`). One level: a projection copied into a second object is not followed.

## Use instead

- A refusal or blocker: build the copy from its typed fields (`code`, `field`, an enum such as `DeletionBlocker.type`) through `t(key, { name })`.
- A failed read or write: `localizedErrorMessage(error, fallback)`, or `settleMutation` and its `failure.body`. Resolve it in the feature hook and hand the screen a string.
- A notification: `getNotificationCopy(notification, t)` builds the title and message from `type` and the `payload` names.
- A field with no structured data beside it: a generic sentence for its type or context, never the server's text.

## Why

The client sends no `Accept-Language`, so a server `message` is English in every locale, and its wording is not a contract (`"1 active collaborator(s)"`). `sous-chef/no-error-message-branching` stops code branching on it and `sous-chef/no-untranslated-toast` covers toast arguments by syntax; this rule reads types, so it also sees JSX and a message that passes through a variable.

## Server-copy fields

Every schema `String` field was classified against `sous-chef-api`. A row is a field the server fills with English it wrote itself, or that a moderator wrote; paths are under `sous-chef-api/packages/`.

| Field                                                                     | Who writes the English                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Structured data to localize from                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Notification.title`, `Notification.message`                              | `core/src/services/notification/NotificationFactory.ts` (`title: "Home Invitation"`, `` message: `${inviterName} invited you to join "${homeName}"` ``, one per collaboration type); `core/src/services/shopping/ShoppingListService.ts` (`"Shopping List Completed"`); `core/src/services/notification/SystemNotificationService.ts` (`"Low Stock Alert"`); `core/src/services/jobs/processors/expirationCheckProcessor.ts` (`"Expiration Reminder"`, the weekly digest); `schema/src/resolvers/notifications/notifications.mutations.ts` (`"Test Notification"`) | `type` plus `payload`: `inviterName`/`homeName`, `listName` and the actor name, `eventType`, `itemName`/`currentQuantity`, `itemName`/`daysUntilExpiry`, `itemCount`/`itemNames`, `test`. An admin's `bulkSendNotifications` carries free text and no payload names; such a row sets `isAuthoredContent: true` and is shown as written (see Exempt). |
| `UserEvent.reason`                                                        | `core/src/subscriptions/publishers/UserPublisher.ts` forwards a moderator's own reason, or null                                                                                                                                                                                                                                                                                                                                                            | `reasonCode` (`FAILED_LOGIN_ATTEMPTS` / `MODERATOR`) and `failedLoginCount`, plus `subtype`, `actorUserId`, `warningCount`                                                                                                                                                                               |
| `MyModerationStatus.banReason` / `suspensionReason` / `restrictionReason` | `core/src/services/user/UserModerationService.ts` (the automatic suspension text, or a moderator's input)                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `banReasonCode` / `suspensionReasonCode` / `restrictionReasonCode`, plus `status`, `isBanned`, `isSuspended`, `restrictions`                                                                                                                                                                                                  |
| `ShoppingListItemSource.autoAddReason`                                    | `core/src/services/pantry/PantryShoppingIntegrationService.ts` (`` `Low stock in ${pantryName} (${currentQuantity} remaining)` ``, `` `Added from ${pantry.name}` ``)                                                                                                                                                                                                                                                                                                                                                                                              | `isAutoAdded`, `addedContext` (`LOW_STOCK`)                                                                                                                                                                                                                          |
| `LedgerPeriodData.periodLabel`                                            | `core/src/services/pantry/analytics/ledgerAnalyticsShape.ts` (the UTC bucket key, `2026-09-14` / `2026-09`)                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `periodStart`, `periodEnd`, and the requested granularity                                                                                                                                                                                                            |
| `AggregationResult.displayText`, `ConversionResult.displayText`           | `core/src/services/item/UnitConversionService.ts` (`` `${value} ${unit.symbol}` ``)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `quantity` / `value` and `unit`                                                                                                                                                                                                                                      |
| `BatchAddShoppingListItemResult.error`                                    | `core/src/services/shopping/ShoppingListItemBatchService.ts` (`getErrorMessage(error) \|\| "Failed to add item"`)                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `code` (set exactly when `success` is false) and `errorId`, plus `success`, `index`                                                                                                                                                                                                                                          |
| `BatchUpsertItemResult.error`                                             | `core/src/services/externalSource/ExternalSourceService.ts` (`error: getErrorMessage(error)`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `code` (set exactly when `success` is false) and `errorId`, plus `success`, `created`                                                                                                                                                                                                                                        |
| `ConsumptionFailure.reason`, `SkippedRecipeIngredient.reason`             | `core/src/services/pantry/PantryDeductionService.ts` via `core/src/utils/errors/payloadFailureReason.ts` (`sanitizeErrorMessage(error.message)`)                                                                                                                                                                                                                                                                                                                                                                                                                   | `code` (`INSUFFICIENT_QUANTITY`, or `UNIT_INVALID` for an absent conversion route) and `errorId`, plus `requestedQuantity` / `availableQuantity`, `fromUnitId` / `toUnitId`, `itemName`                                                                                                                                                                                     |
| `FailedMoveInfo.reason`                                                   | `core/src/services/shopping/ShoppingToPantryService.ts` via `payloadFailureReason.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `code` (now the `ErrorCode` enum, not a String) , `errorId`, `itemName`                                                                                                                                                                                                                                        |
| `SkippedLowStockItem.reason`                                              | `core/src/services/pantry/PantryShoppingIntegrationService.ts` (`"Item already in shopping list"`, a caught `error.message`)                                                                                                                                                                                                                                                                                                                                                                                                                                       | `code` (`ITEM_ALREADY_IN_LIST`) and `errorId`, plus `itemName`, `pantryItemId`                                                                                                                                                                                                                                  |
| `DuplicatePantryItemError.suggestion`                                     | `core/src/errors.ts` (`"RESTOCK_EXISTING"`), passed through `core/src/utils/mutations/handleMutationError.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `code`, `existingPantryItemIds`                                                                                                                                                                                                                                      |
| `ItemValidationWarning.suggestion`                                        | `schema/src/resolvers/item/item.queries.ts` (`"Add a UPC for barcode scanning support"`, …)                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `code: ItemValidationWarningCode!` (`MISSING_DESCRIPTION` / `MISSING_UPC` / `MISSING_CATEGORY` / `MISSING_UNIT`) and `field`. Unadopted: no client operation selects this type. |
| `ShoppingListActivity.description`                                        | `core/src/services/shopping/ShoppingListActivityService.ts` (`` `${actorName} cleared ${count} item${count === 1 ? "" : "s"} from the list` ``, `` `${creatorName} created shopping list "${listName}"` ``)                                                                                                                                                                                                                                                                                                                                                        | `action` plus typed fields per action (`listName`, `changedFields`, `quantity`, `itemCount`, `purchasedOnly`, `collaboratorEmail`, `collaboratorRole`, `previousRole`, `newRole`), `itemName`, `oldValue` / `newValue`, `user`. Unadopted: `activitiesConnection` is admin-only and no client operation selects this type. |
| `InviteLog.description`                                                   | an audit sentence stored by `core/src/repositories/invite/InviteLogRepository.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `action`, `oldStatus` / `newStatus`, `actor` — already structured. Unadopted: no client operation selects this type, and no server path writes these rows today. |

`message` on every generated type (`DeletionBlocker`, `SyncConflictInfo`, `BulkDeviceFailure`, the `Error` implementers, the auth payloads) is covered by the rule's first half and is not repeated here.

### Classified and left out

- **Mixed: typed by a person, with a kind saying which.** `PantryItemUsage.adjustmentReason` is the reason the adjust and correct-weight forms send, except on a row the server wrote itself. `PantryItemUsage.adjustmentKind` (`USER` / `STACK_MERGE` / `STACK_PURGED` / `STACK_REMOVED`) says which: `UsageHistoryRow` renders local copy for the three server kinds and shows a `USER` row's reason as written. The kind rides every correction row in BOTH directions, a `RESTOCK` row included, so it — not the purpose — is what marks a line as a correction.
- **Codes, not copy.** `LoginEvent.failureReason`, `triggerEvent` and `riskFactors` carry enum-like identifiers (`RATE_LIMITED`, `failed_attempt`); `ItemPhoto.perspective`, `Recipe.source`, `ItemImage.source` likewise. Rendering one raw is an enum-formatting problem, not server prose.
- **Identifiers and machine values.** `HomeOwnership.transferredFrom` / `ShoppingListOwnership.transferredFrom` (a user id), `PantryItemChange.oldValue` / `newValue` (ids and JSON), `QuantityDisplay.display` / `fraction` / `mixed` (numerals), `ShareLink.universal`, every `cursor`, `url`, token and id.
- **Catalog reference data.** `Item.description`, `Category.description`, `Brand.description`, `HealthBenefit.benefit` / `description` / `scientificEvidence`, `ItemUnit.displayNameSingular` / `displayNamePlural`, `SourceAllergens.contains` / `mayContain`: imported product data, English like the item names beside it, and outside this rule.
- **User content.** Names, notes, `tips`, `description` on `Home`, `Pantry`, `ShoppingList`, `Recipe`, `MealPlan`, `MealTemplate` and `StorageLocation`, `HomeInvite.message`, `RecipeReview.comment`, `ItemEditSuggestion.note`, `MyModerationStatus.appealNotes`.

## Exempt

- **An authored notification.** `Notification.isAuthoredContent` is `true` only when a PERSON wrote the title and message — an admin's announcement through `bulkSendNotifications` or `createNotification`. Those two fields are then content, like a list's name, and may be rendered where a reachable condition has established the flag: `n.isAuthoredContent ? n.title : …`, `n.isAuthoredContent && n.message`, an `if` whose consequent holds the read, or the early-return guard `if (!n.isAuthoredContent) return null;` earlier in the same block. The ELSE arm of such a test is still reported — that is the templated row, whose copy is the English this rule exists to withhold — and the exemption covers `title` and `message` only. `getNotificationCopy` takes the branch centrally, so a screen rarely needs to.
- react-hook-form's `fieldState.error?.message` / `formState.errors.x?.message`: typed `FieldError` from react-hook-form, carrying the form's own yup copy.
- An `Error` the app built itself, and any value typed `Error` or `unknown`. A hook that widens Apollo's `ErrorLike` to `Error` in its return type hides the source from this rule, so resolve the copy inside the hook.
- A server-copy field or `message` copied into an app-declared object (`{ title: n.title }` returned from a projection), unless `followProjections` is on; either way a projection rendered in another file is not seen. Build the copy where the generated type is still in hand.
- Logging and reporting: a read that reaches no output.
- Test files and `.graphql` documents.
- Files without type information.

Source: [`eslint/plugin/rules/no-rendered-server-message.js`](../../eslint/plugin/rules/no-rendered-server-message.js) · spec: [`__tests__/lint/rules/no-rendered-server-message.test.ts`](../../__tests__/lint/rules/no-rendered-server-message.test.ts)
